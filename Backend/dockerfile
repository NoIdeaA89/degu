FROM node:24-alpine AS dev

WORKDIR /app

# Copia package.json y package-lock.json
COPY package*.json ./

# Instala todas las deps (incluyendo dev)
RUN npm install

COPY . .

EXPOSE 3000

# CMD para desarrollo
CMD ["ts-node-dev", "--transpile-only", "src/index.ts"]
